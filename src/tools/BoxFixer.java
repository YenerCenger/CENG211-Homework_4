package tools;

import game.BoxPuzzle;

public class BoxFixer extends SpecialTool {
    @Override
    public void useTool(BoxPuzzle puzzle) {
        // TODO: ask location; replace with FixedBox (throw BoxAlreadyFixedException if already FixedBox)
    }
}
